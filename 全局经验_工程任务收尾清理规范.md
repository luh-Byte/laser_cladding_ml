# 全局经验：工程任务收尾清理规范

> 原则：每一个工程任务运行完成后，必须确保任务中不留存非必要的上下文噪声、历史返工噪声，仅保留正向流程。

---

## 一、为什么要做收尾清理

1. **可读性**：新项目成员打开代码库，看到的应该是当前正确状态，而非"曾经尝试过A/B/C方案"的痕迹
2. **可维护性**：废弃代码、遗留配置会误导后续修改，增加维护成本
3. **可复现性**：输出目录中只保留当前版本的结果，避免混淆"哪个是最新的"
4. **专业度**：干净的代码库本身就是工程质量的体现

---

## 二、清理检查清单（5个维度）

### 1. 代码文件清理

**必须检查**：

| 检查项 | 操作 | 判定标准 |
|--------|------|---------|
| 未使用的导入 | 删除 | 全库grep该符号，无引用即删除 |
| 未使用的函数/类 | 删除 | 全库grep调用点，无调用即删除 |
| 未使用的常量/配置 | 删除 | 全库grep引用点，无引用即删除 |
| 注释掉的代码块 | 删除 | 版本历史已由git/运行记录保留 |
| TODO/FIXME标记 | 处理或删除 | 不遗留悬而未决的标记 |
| 调试打印语句 | 删除或改为日志级别 | 不残留 `print("debug xxx")` |
| 硬编码的临时路径/参数 | 改为配置项或移除 | 不残留测试用的硬编码值 |

**检查方法**：
- 对每个函数/常量，grep全库确认被调用
- 对每个import，确认被当前文件使用

### 2. 配置文件清理

**必须检查**：

| 检查项 | 操作 |
|--------|------|
| 废弃的配置项 | 删除（不在代码中引用的） |
| 多次迭代的参数注释 | 只保留最终版本的说明 |
| 调试用开关 | 关闭或删除 |
| 章节编号 | 连续不跳号 |

**原则**：配置文件反映的是"当前是什么"，不是"曾经试过什么"。
迭代历史应在日志或实验记录中体现，不应残留在配置常量里。

### 3. 输出文件清理

**必须检查**：

| 检查项 | 操作 | 判定标准 |
|--------|------|---------|
| 失败/中断运行的输出 | 删除 | 报错日志、部分结果、空文件 |
| 上一轮迭代的模型文件 | 删除 | 只保留当前最优模型 |
| 上一轮迭代的结果文件 | 删除或覆盖 | 只保留当前版本的汇总表 |
| 临时调试文件 | 删除 | .tmp、.bak、debug_xxx等 |
| 字节码缓存 | 删除 | `__pycache__`、`.pyc`文件 |
| 空目录 | 删除 | 无文件的目录不应保留 |

**原则**：输出目录应该是"运行一次就能得到的完整结果"，
不应包含多次运行的历史堆积。

### 4. 数据文件清理

| 检查项 | 操作 |
|--------|------|
| 中间数据文件 | 删除（原始数据应保留） |
| 测试用的小样本数据 | 删除 |
| 重复的数据副本 | 删除，保留一份 |

### 5. 文档注释清理

| 检查项 | 操作 |
|--------|------|
| 描述过时的注释 | 更新为当前状态 |
| 记录"为什么这么做"的注释 | 保留（有价值的上下文） |
| 记录"曾经试过什么"的注释 | 删除 |

---

## 三、清理操作的执行顺序

```
1. 代码检查
   ├─ 逐个文件检查未使用的import/函数/常量
   ├─ 删除废弃代码和调试语句
   └─ 更新过时注释

2. 配置检查
   ├─ 确认所有配置项被引用
   ├─ 确保配置反映最终状态
   └─ 章节编号连续

3. 输出清理
   ├─ 删除失败运行的残留
   ├─ 删除上一轮迭代的模型/结果
   ├─ 删除临时文件和缓存
   └─ 删除空目录

4. 验证运行
   └─ 运行一次完整正向流程，确认结果正确

5. 最终确认
   ├─ 列出所有文件，确认无冗余
   └─ 核心指标与清理前一致（证明未误删）
```

---

## 四、本次项目清理实例

**清理前问题**：
- `config.py` 中有 `RESULT_COLUMNS`、`CORROSION_RESULT_COLUMNS` 两个未使用的常量
- `data_preprocessing.py` 中有 `get_feature_columns()` 未使用的函数
- 导入了 `TARGETS` 但未使用
- `outputs/models/` 中有上一轮迭代残留的 `model_RF_硬度.pkl`（当前最优是LightGBM）
- `outputs/training_log.txt` 是第一次失败运行的日志
- `src/__pycache__/` 字节码缓存

**清理后状态**：
- 代码：6个模块，全部函数/常量均在使用
- 输出：仅保留当前版本的模型和结果
- 验证：完整运行10次重复实验，结果与清理前一致

---

## 五、关键提醒

1. **先验证，再删除**：删除前确认该文件/代码确实不在使用中
2. **最终验证必须做**：清理后必须运行一次完整流程，确保没有误删
3. **配置是唯一真相源**：所有参数应集中在配置文件，代码中不应有散落的硬编码
4. **迭代历史不进代码**："试过什么、为什么改"应在日志/文档中，不应残留在代码注释里
5. **输出只留最新版**：结果文件每次运行覆盖，不堆积历史版本

---

## 六、可视化绘图经验

### 1. 字体配置

- 论文图表统一使用 Times New Roman 兼容字体（Linux 环境用 Liberation Serif 度量替代）
- 全局通过 `rcParams` 设置，不逐图指定
- 字体文件需显式注册：`fm.fontManager.addfont(path)`
- 保存前强制全文字体加粗：遍历 `fig.findobj(Text)` 逐一设置

### 2. 离群点处理

- 离群点应在数据清洗阶段直接从数据中删除，而非在绘图时过滤
- 数据清洗规则需覆盖两种孤立点：
  - 异常值（如功率≈5000W且硬度>1500HV）
  - 空间孤立点（如功率>=5500W，与其他数据间断过大）
- 绘图函数可保留大间隔自动检测作为安全保险，但数据层删除是第一防线

### 3. 特征名简写映射

- 图表轴标签使用简写避免拥挤，代码中维护简写↔完整名双向映射
- `FEATURE_ABBR`：简写 -> 完整名（如 "LP" -> "Laser Power"）
- `FEATURE_EN_TO_ABBR`：完整名 -> 简写（反向查找）
- 显示时用简写，论文注释或图注中附完整名映射表
- 同一映射字典在 `plot_figures.py` 和 `plot_shap.py` 中保持一致

### 4. 画布与样式统一

- 统一三种画布尺寸：常规 8×6 / 双子图 14×6 / 热图 10×8
- 渐变背景通过 `ax.imshow` + `transform=ax.transAxes` 实现
- `create_gradient_rect` 保存并恢复 xlim/ylim，避免被 imshow 覆盖
- `style_ax` 统一图框、刻度、字体，所有图共用

### 5. 防止元素重叠

- 数据标签与图形元素重叠时：调整 `label_y` 偏移量、缩小字号
- 图例放置：优先 `frameon=False` 无边框，位置根据数据分布选择
- 堆叠柱状图标签：可竖向排列在图框内右侧（`loc="center right"`）
- 密集标注区域：用最近邻距离过滤，仅标注稀疏点

---

*本规范适用于所有工程类任务。每个任务完成后，按此清单执行收尾清理。*
